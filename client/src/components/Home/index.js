import React from "react";
import styled from "styled-components";
import { spacing64 } from "../../resources/style-constants";

const Wrapper = styled.div`
  padding: ${spacing64};
`;

const Home = () => {
  return <Wrapper>hola</Wrapper>;
};

export default Home;
