import React from "react";
import styled from "styled-components";

const MapPin = ({ text, lat, lng }) => {
  return (
    // <div className="pin">
    //   <Icon icon={locationIcon} className="pin-icon" />
    //   <p className="pin-text">{text}</p>
    // </div>
    <div></div>
  );
}

export default MapPin;
